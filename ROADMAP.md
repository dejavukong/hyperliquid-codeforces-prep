# 逐题详解 · 从易到难

> 每题结构：**题意**（来自真实题面）→ **为什么和 HL 相关** → **关键算法/数据结构** → **面试会怎么追问**。
> 难度为 CF 官方 rating，tags 为官方标签。

---

## 模块 A · 热身：贪心、排序、哈希（建立"撮合/分配"直觉）

### 1. [230A — Dragons](https://codeforces.com/problemset/problem/230/A) · 1000 · `greedy, sortings`
**题意**：你初始力量为 `s`，要打败 `n` 条龙，第 `i` 条龙力量 `x_i`、击败后奖励 `y_i`。若你的力量 **> 龙的力量**则获胜并 `+y_i`，否则死亡。可任意顺序，问能否全部击败。
**为什么和 HL 相关**：这是**清算/处理顺序**的最小模型——按"门槛"从低到高排序，先处理你当前能力覆盖的对象，吃到奖励再去处理更硬的。清算引擎按维持保证金缺口/风险从低到高扫一遍是同一思路。
**关键**：按 `x_i` 升序排序后顺序模拟。贪心可行性证明：能打则打一定不劣。
**面试追问**：为什么贪心正确？如果"奖励可能为负"（处理一个清算反而消耗保证金）顺序还成立吗？

### 2. [176A — Trading Business](https://codeforces.com/problemset/problem/176/A) · 1200 · `greedy, sortings`
**题意**：`n` 个星球、`m` 种商品。星球 `i` 商品 `j` 有买入价 `a_ij`、卖出价 `b_ij`、库存上限 `c_ij`。在**一个**星球买、运到**另一个**星球卖（只做一次），船舱最多装 `k` 件，求最大利润。
**为什么和 HL 相关**：跨场所**价差套利 + 有限容量约束下的最优配置**。对固定(买星球→卖星球)，每件商品利润 = `b_卖 - a_买`，把利润最高的商品优先装满到 `k`——这正是有限保证金/库存下"把额度分配给收益最高腿"的雏形。
**关键**：枚举买卖星球对 O(n²)，对每对按单件利润降序贪心装 `k` 件。
**面试追问**：如果允许多次买卖、或容量换成保证金（不同商品占用不同保证金），就变成背包了——见第 13 题。

### 3. [4C — Registration System](https://codeforces.com/problemset/problem/4/C) · 1300 · `data structures, hashing`
**题意**：注册系统，每次来一个名字：库里没有则插入并回 `OK`；已存在则在名字后追加最小未用数字 `name1/name2/...` 返回并入库。
**为什么和 HL 相关**：撮合引擎里 `order_id → order`、`user → account` 都是**哈希表 O(1) 查重/定位**。订单的注册、去重、生成唯一标识就是这个。
**关键**：`unordered_map<string,int>` 记录每个名字已用到的后缀计数。
**面试追问**：哈希冲突/最坏退化怎么办？高频撤单下哈希表 rehash 抖动如何避免？（→ 引出预分配、开放寻址 vs 链式）

---

## 模块 B · 订单簿核心：有序结构、二分、单调栈

### 4. [91B — Queue](https://codeforces.com/problemset/problem/91/B) · 1500 · `binary search, data structures`
**题意**：队列中 `n` 个人，第 `i` 个人年龄 `a_i`。对每个 `i`，找**他前方（下标更大）最远的、比他年轻的人**，输出两人之间的人数（找不到为 -1）。
**为什么和 HL 相关**："对每个订单，找右侧最远的更优价位"——本质是**后缀最小值 + 二分定位**。订单簿里判断一个挂单能否被后续更优价吃到、扫描可成交边界都用得上。
**关键**：从右往左维护后缀最小值数组（单调），对每个 `i` 在后缀最小值上**二分**最远满足 `min < a_i` 的位置。
**面试追问**：为什么后缀最小值单调、从而能二分？能否用单调栈做到 O(n)？

### 5. [68B — Energy exchange](https://codeforces.com/problemset/problem/68/B) · 1600 · `binary search`
**题意**：`n` 个蓄电池各有能量 `a_i`，能量可互相转移，但每转移 `x` 单位**损耗 `k%`**。求转移后所有电池能达到的**最大相等能量**。
**为什么和 HL 相关**：**资金利用效率的核心模型**——把资金/能量在账户间再平衡，调拨有滑点/手续费（损耗），求约束下能达到的最优均衡水平。这就是 cross-margin 再分配、跨子账户调拨的算法骨架。
**关键**：**二分答案** `mid`（目标水平）。高于 `mid` 的电池贡献 `(a_i - mid)·(1-k%)`，低于的需求 `(mid - a_i)`，可行当供给 ≥ 需求。答案单调 → 二分。
**面试追问**：为什么"可行性"对目标水平单调？精度/浮点怎么控制（eps、迭代次数）？这正是面试官最爱考的"二分答案"母题。

### 6. [339D — Xenia and Bit Operations](https://codeforces.com/problemset/problem/339/D) · 1700 · `data structures (segment tree)`
**题意**：长度 `2^n` 的序列，自底向上两两合并：第一层 OR、第二层 XOR、再 OR、再 XOR……交替，最终得到根值 `v`。`m` 次操作：每次**单点修改**一个元素，输出新的 `v`。
**为什么和 HL 相关**：这是**线段树点更新 + 自底向上 pushup** 的纯净版。订单簿某价位挂单量变化后，要 O(log n) 增量重算总深度/加权净额/聚合指标——结构完全一样（只是合并算子从 OR/XOR 换成 sum/max）。
**关键**：满二叉线段树，叶子改一个，沿到根重算 `log n` 个节点，按层奇偶选 OR/XOR。
**面试追问**：合并算子需要满足什么性质才能这样增量更新（结合性）？如果要支持区间查询而非全局根值呢？

### 7. [4D — Mysterious Present](https://codeforces.com/problemset/problem/4/D) · 1700 · `dp, sortings`
**题意**：`n` 个信封，每个有宽 `w_i`、高 `h_i`。要选最长的链，使链中信封宽和高都**严格递增**，且链中最小信封要能装下给定卡片（`w>w0, h>h0`）。输出最长链及方案。
**为什么和 HL 相关**：**二维偏序下的最长可行链（2D-LIS）**。当一个操作要同时满足多重单调约束（比如同时满足多个保证金/限额维度才能嵌套执行）时，求最长可行操作序列就是这个模型。
**关键**：先过滤装得下卡片的信封，按 `w` 排序后对 `h` 求 LIS（DP `O(n²)` 或树状数组优化到 `O(n log n)`），记录前驱还原路径。
**面试追问**：如何从 O(n²) 优化到 O(n log n)？相等维度怎么处理避免误判严格递增？

### 8. [459D — Pashmak and Parmida's problem](https://codeforces.com/problemset/problem/459/D) · 1800 · `data structures (BIT), sortings`
**题意**：序列 `a`，定义 `f(l,r,x)` = 区间 `[l,r]` 内等于 `x` 的个数。求满足 `i<j` 且 `f(1,i,a_i) > f(j,n,a_j)` 的下标对 `(i,j)` 数量。
**为什么和 HL 相关**：**树状数组数偏序对**的代表题。统计"满足某条件的 (买单, 卖单) 对数""有多少风险事件先于另一类发生"——都是在一维上数偏序，BIT 是标准武器。
**关键**：预处理前缀计数 `L_i` 和后缀计数 `R_j`，问题化为数 `i<j, L_i>R_j` 的对——从左到右扫，用 BIT 维护已出现的 `R` 值，查询比当前 `L_i` 小的个数。
**面试追问**：为什么用 BIT 而不是排序双指针？值域大要离散化，怎么做？

### 9. [46D — Parking Lot](https://codeforces.com/problemset/problem/46/D) · 1800 · `data structures (segment tree), implementation`
**题意**：长 `L` 的街道停车，车来时要求**与后车间距 ≥ b、与前车间距 ≥ f**（无车则可贴边）。两类事件：车驶入（找**最左**可停位置，停不下输出 -1）、车驶离。在线处理。
**为什么和 HL 相关**：**空位/区间分配**的典型——订单簿的空价位、撮合引擎的内存池/对象池、落盘的 slot 分配，都要 O(log n) 找最左可用空隙并支持插入/释放。
**关键**：把已停的车按位置维护在有序结构里，用**线段树/有序集合**在间隙中找第一个能容纳新车（满足 b、f）的位置；驶离则删除。
**面试追问**：用 `std::set` 维护占用区间 + 在相邻车之间检查间隙，复杂度？最左优先如何保证？高频进出下如何避免碎片？

### 10. [547B — Mike and Feet](https://codeforces.com/problemset/problem/547/B) · 1900 · `binary search, data structures, dp, dsu`
**题意**：`n` 头熊排成一行，高度 `a_i`。一个"组"是连续区间，组的强度 = 区间最小高度。对每个组大小 `x (1..n)`，求所有大小为 `x` 的组的**最大强度**。
**为什么和 HL 相关**：**单调栈**把 O(n²) 干到 O(n) 的经典案例——这正是 HL 看重"高性能"的考点：对每个区间长度求最大的最小值 = 深度-价格曲线、各窗口的最差风险极值。能不能想到单调栈是分水岭。
**关键**：对每个元素用**单调栈**求它作为最小值能向左右扩展的最大宽度 `len`，则它能更新 `ans[len]`，最后从大到小后缀取 max。
**面试追问**：单调栈为什么 O(n)（每个元素进出栈各一次）？为什么要对答案数组做后缀 max？

---

## 模块 C · 撮合与状态：堆+链表、区间覆盖、可合并线段树

### 11. [45C — Dancing Lessons](https://codeforces.com/problemset/problem/45/C) · 1900 · `data structures (heap + linked list)`
**题意**：`n` 人排成一行，技能 `a_i`。只要还存在相邻一男一女，就让**相邻、技能差最小**的一对（并列取最左）出列跳舞，队列随即闭合（两侧接上）。输出配对顺序。
**为什么和 HL 相关**：**这就是撮合引擎本体**。`priority_queue`（按差值/价格取最优对）+ **双向链表**（维护"相邻"关系，撮合后把两侧接上并产生新相邻对入堆）+ **惰性删除**（已出列的对在堆里标记失效）——price-time priority、撤单失效、撮合后重连，全在这一题里。
**关键**：所有相邻对入小根堆（键：差值，平局取左下标）；双向链表 `prev/next` + `alive[]`；弹堆时跳过失效对，配对后删两点、连接它们的左右邻居、把新相邻对入堆。
**面试追问**：堆里失效元素怎么处理（惰性删除 vs 可删堆）？为什么这是 O(n log n)？这题强烈建议吃透——它和真实撮合最像。

### 12. [292E — Copying Data](https://codeforces.com/problemset/problem/292/E) · 1900 · `data structures (segment tree)`
**题意**：两个数组 `a`、`b`。操作一：把 `a` 从位置 `x` 起、长 `k` 的子段拷到 `b` 从位置 `y` 起（`b[y+q]=a[x+q]`）。操作二：查询 `b[x]`。
**为什么和 HL 相关**：**区间覆盖 + 最新写胜出**——状态快照/落盘的模型：批量区间写入后点查"当前值"，谁写得晚谁有效。版本化 ledger、增量快照都是这个结构。
**关键**：线段树带 lazy，记录每段"最近一次拷贝的来源偏移与时间戳"；查 `b[x]` 时看覆盖它的最新写，命中则映射回 `a`，否则取原 `b`。
**面试追问**：如何保证"最新写覆盖旧写"（时间戳/惰性下推顺序）？和持久化/版本化的关系？

### 13. [19B — Checkout Assistant](https://codeforces.com/problemset/problem/19/B) · 1900 · `dp`
**题意**：`n` 件商品，第 `i` 件价格 `c_i`、收银员处理耗时 `t_i` 秒。收银员处理某件的 `t_i` 秒内，Bob 可偷走另外 `t_i` 件（偷一件 1 秒）。求买下全部商品的**最小花费**。
**为什么和 HL 相关**：**背包变形**——选一件 `i` "覆盖" `t_i+1` 个名额（它自己 + 偷的 `t_i` 件），要用最小总价覆盖全部 `n` 个名额。这是**有限处理时间/算力预算下的最优资金分配**：每笔操作消耗预算并产出，求覆盖所有需求的最小成本。
**关键**：0/1 背包，`dp[j]` = 处理掉 `j` 件的最小花费，物品 `i` 的"体积" = `min(t_i+1, n)`，价值 = `c_i`，求 `dp[n]`。
**面试追问**：为什么体积要对 `n` 取 min？这和经典 0/1 背包的"恰好装满"边界如何对应？

### 14. [61E — Enemy is weak](https://codeforces.com/problemset/problem/61/E) · 1900 · `data structures (BIT)`
**题意**：序列 `a`（各元素互不相同），求**三元逆序对**数量：`i<j<k` 且 `a_i>a_j>a_k`。
**为什么和 HL 相关**：**多腿/级联事件计数**。清算级联里"先后发生且强度递减的三连击"、多腿套利里满足顺序+大小关系的组合计数，都是三元（乃至多元）偏序，BIT 两遍是标准解法。
**关键**：对每个 `j`，求左边比它大的个数 `Lg_j` 和右边比它小的个数 `Rs_j`，答案 `= Σ Lg_j · Rs_j`。两遍 BIT（离散化后）。
**面试追问**：从二元逆序对（归并/BIT）如何推广到三元？复杂度 O(n log n) 的瓶颈在哪？

### 15. [380C — Sereja and Brackets](https://codeforces.com/problemset/problem/380/C) · 2000 · `data structures (segment tree)`
**题意**：括号串 `s`，`m` 个查询 `(l,r)`，求子串 `s[l..r]` 的**最长合法括号子序列**长度。
**为什么和 HL 相关**：**可合并区间信息的线段树**——撮合的精髓是"区间内能配对多少"。每个线段树节点存 `(已匹配数, 剩余未匹配的左括号, 剩余未匹配的右括号)`，合并时左边的多余 `(` 和右边的多余 `)` 再配对。这正是**区间可成交量/净额聚合查询**：买卖在区间内能撮合掉多少。
**关键**：定义可合并三元组 + pushup 规则 `matched += min(left.open, right.close)`；查询区间合并若干节点。
**面试追问**：合并规则为什么正确、是否满足结合律？这套"定义可合并状态 + pushup"是线段树进阶的通用范式，务必讲清。

---

## 模块 D · 压轴：分片 / 负载均衡

### 16. [2046C — Adventurers](https://codeforces.com/problemset/problem/2046/C) · 2100 · `binary search, data structures, greedy, sortings, two pointers`
**题意**：平面上若干点（城市）。选一个分割点 `(x0,y0)` 把平面分成四个象限，四个商人各占一个象限。求一种 `(x0,y0)` 使**四个象限中点数最少的那个尽量大**（最大化最小象限）。
**为什么和 HL 相关**：**分片 / 负载均衡**——把订单簿/账户按二维（如价格×时间、或两类风险维度）切分成多个 shard，使最繁忙的 shard 负载最小化。HL 这种高吞吐引擎做水平扩展时，正是这种"切分使最坏分片最优"的问题。
**关键**：离散化坐标，扫描线枚举 `x0`，用**合并排序树 / 可持久化线段树**维护当前左右两侧的 `y` 分布，对每个 `x0` 在 `y` 上二分使四象限的最小值最大。
**面试追问**：为什么"最大化最小值"暗示二分/贪心扫描？合并排序树和可持久化线段树各自的时空权衡？这是综合性压轴，串起了二分+扫描+可合并结构。

---

## 刷完之后

- 用 CF API 按 `data structures` + rating `1400~2000` 自助扩展，优先选 tag 同时含 `data structures, greedy/sortings` 的题（最贴近撮合/清算）。
- 重点反复练：手写**线段树**（点改/区间改/可合并）、**树状数组**、`std::set`/`std::priority_queue` 的撮合式用法、**单调栈/队列**、**二分答案**。
- 每道都练到能**口头讲清数据结构选择与复杂度**——HL 面试官会顺着追问到工程实现细节。
